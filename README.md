# Loan_Reommender_System

### Start the server:
For running the chatbot, we should go to rhe exact folder that main_chatbot exists. In this case go to deployment_extraction_chatbot
uvicorn main_chatbot:app --reload --host 0.0.0.0 --port 8000

### Check it in browser:

http://localhost:8000/docs ## FastAPI 
